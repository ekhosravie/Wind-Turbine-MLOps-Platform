"""
Model Serving deployment. The original notebook only provides this as a markdown guide, not executable code - creating a live Serving endpoint needs a deployed workspace/token and isn't something that runs unattended in a notebook cell. Placeholder retained intentionally.
"""

raise NotImplementedError(
    "Model Serving deployment was delivered as a markdown guide in the "
    "original notebook, not executable code. See README.md 'Model Serving' "
    "section for the Databricks SDK call to fill in here, plus your "
    "workspace's serving-endpoint token/permissions, before running this."
)
